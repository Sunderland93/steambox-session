#!/bin/bash

xsetroot -solid black &
metacity --replace --no-composite &

which nvidia-settings && nvidia-settings -l 
which gnome-settings-daemon && gnome-settings-daemon &

cd /usr/share/steambox-manager || exit 1
exec ./manager.py
